# 1.分解质因数。

```js
function f1(num) {
  var num1 = num;
  var result = [];
  var i = 1;

  while (i < num1) {
    i += 1;
    while (num1 % i == 0) {
      num1 /= i;
      result.push(i);
    }
  }

  return result;
}
```
